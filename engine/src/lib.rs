#![deny(warnings)]

use ::dioxus::prelude::Asset;
use ::std::collections as ds;
use ::std::ops;

::modwire::expose!(
    pub bus
    pub effect
    pub identity
    pub service_id
    pub service_package
    pub service
    pub sprite
);